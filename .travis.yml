sudo: required

services:
    - docker

script:
    - docker build -t aberwaru/waru-maintenance-docker .
    - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
    - docker push aberwaru/waru-maintenance-docker

notifications:
    email:
        on_success: change
        on_failure: change
